# 🧠 ML Модели бота: Как они работают и обучаются

## 📚 Оглавление
1. [Структура ML системы](#структура-ml-системы)
2. [Как обучались модели](#как-обучались-модели)
3. [Текущее состояние моделей](#текущее-состояние-моделей)
4. [Как дообучить модели](#как-дообучить-модели)
5. [Онлайн обучение vs Переобучение](#онлайн-обучение-vs-переобучение)

---

## 🎯 Структура ML системы

### 3 модели работают вместе (Ensemble):

```
┌─────────────────────────────────────────────────┐
│           ENSEMBLE PREDICTOR                    │
└─────────────────────────────────────────────────┘
                    ↓
    ┌───────────────┬───────────────┬──────────────┐
    │               │               │              │
┌───▼────┐    ┌────▼─────┐   ┌─────▼──────┐
│ Random │    │   LSTM   │   │ Logistic   │
│ Forest │    │  Neural  │   │ Regression │
│        │    │  Network │   │            │
└────┬───┘    └─────┬────┘   └──────┬─────┘
     │              │               │
     │ Голосование (Weighted Vote) │
     └──────────────┼───────────────┘
                    ↓
            BUY / SELL / HOLD
              + Confidence %
```

---

## 🔍 Подробно о каждой модели

### 1️⃣ Random Forest (Случайный лес)

**Файл:** `src/ml/predictor.py`

**Что это:**
- Ансамбль из 100 деревьев решений
- Каждое дерево голосует за BUY/SELL/HOLD
- Итоговое решение = большинство голосов

**Входы (Features):**
```python
features = [
    'RSI',              # Индикатор перекупленности/перепроданности
    'ATR',              # Волатильность
    'volume',           # Объем торгов
    'SMA_50',           # Простая скользящая средняя (50 периодов)
    'SMA_200',          # Простая скользящая средняя (200 периодов)
    'MACD',             # Конвергенция/дивергенция скользящих средних
    'MACD_signal',      # Сигнальная линия MACD
    'BB_upper',         # Верхняя полоса Боллинджера
    'BB_lower',         # Нижняя полоса Боллинджера
    'volume_sma_ratio'  # Отношение текущего объема к среднему
]
```

**Выход:**
- Класс: 0 (DOWN) или 1 (UP)
- Вероятность: 0-100%

**Преимущества:**
- ✅ Быстрая работа
- ✅ Не требует нормализации
- ✅ Показывает важность признаков
- ✅ Устойчива к переобучению

**Параметры:**
```python
n_estimators = 100        # Количество деревьев
max_depth = 15            # Максимальная глубина дерева
min_samples_split = 10    # Минимум примеров для разбиения
class_weight = 'balanced' # Балансировка классов
```

---

### 2️⃣ LSTM Neural Network (Нейронная сеть)

**Файл:** `src/ml/lstm_predictor.py`

**Что это:**
- Рекуррентная нейронная сеть с долгой памятью
- Анализирует **последовательности** данных (60 свечей)
- Понимает паттерны во времени

**Архитектура:**
```
Input Layer (60 свечей × N признаков)
    ↓
LSTM Layer 1 (128 нейронов)
    ↓
LSTM Layer 2 (128 нейронов)
    ↓
Dropout (20%)
    ↓
Fully Connected (64 нейрона)
    ↓
ReLU Activation
    ↓
Output Layer (3 класса: DOWN/HOLD/UP)
    ↓
Softmax → Вероятности
```

**Входы:**
- Последовательность из **60 свечей** (1 час при 1min свечах)
- Для каждой свечи: open, high, low, close, volume + индикаторы

**Выход:**
- Класс: -1 (DOWN), 0 (HOLD), 1 (UP)
- Вероятность для каждого класса

**Преимущества:**
- ✅ Видит временные паттерны
- ✅ Помнит долгосрочные зависимости
- ✅ Лучше для трендов

**Недостатки:**
- ⚠️ Требует больше данных
- ⚠️ Медленнее обучение
- ⚠️ Нужна GPU для быстрой работы (опционально)

**Параметры:**
```python
sequence_length = 60   # Длина последовательности
hidden_size = 128      # Размер скрытого слоя
num_layers = 2         # Количество LSTM слоев
dropout = 0.2          # Dropout для регуляризации
learning_rate = 0.001  # Скорость обучения
epochs = 50            # Количество эпох
batch_size = 32        # Размер батча
```

---

### 3️⃣ Logistic Regression (Логистическая регрессия)

**Файл:** `src/ml/ensemble_predictor.py`

**Что это:**
- Простая линейная модель
- Быстрая и стабильная
- Дополнительное мнение для ансамбля

**Преимущества:**
- ✅ Очень быстрая
- ✅ Интерпретируемая
- ✅ Стабильная

---

## 🎓 Как обучались модели

### Процесс обучения:

```
1. СБОР ДАННЫХ
   ├─ Скачали 2000 свечей с биржи (BTC/USDT)
   ├─ Timeframe: 1h или 15m
   └─ Примерно: 83 дня истории (2000 часов)

2. ПОДГОТОВКА ДАННЫХ
   ├─ Рассчитали индикаторы (RSI, MACD, BB, ATR...)
   ├─ Создали Target: будет ли цена выше через N свечей?
   │  • Target = 1 если price[t+5] > price[t] * 1.005 (>0.5% рост)
   │  • Target = 0 иначе
   └─ Очистили NaN значения

3. РАЗДЕЛЕНИЕ
   ├─ Train: 80% данных (старые)
   └─ Validation: 20% данных (свежие)
   
   ⚠️ ВАЖНО: Для временных рядов НЕ перемешиваем!
   Train всегда РАНЬШЕ Validation по времени!

4. ОБУЧЕНИЕ Random Forest
   ├─ Fit на Train данных
   ├─ 5-Fold Time Series Cross-Validation
   └─ Оценка на Validation

5. ОБУЧЕНИЕ LSTM
   ├─ Создали последовательности по 60 свечей
   ├─ Обучение 50 эпох
   ├─ Early stopping при переобучении
   └─ Сохранили лучшую модель

6. ТЕСТИРОВАНИЕ
   ├─ Walk-Forward Validation
   │  • Обучаем на 30 днях → Тест 7 дней
   │  • Сдвигаем окно → Обучаем снова
   │  • Повторяем для всей истории
   └─ Оценка финальной точности
```

---

## 📊 Текущее состояние моделей

### Где хранятся:

```
models/
├── random_forest_model.joblib       ← Random Forest (обучена)
├── btc_usdt/                        ← Модели для BTC (если обучали)
│   ├── random_forest.joblib
│   ├── lstm_model.pt
│   ├── scaler.pkl
│   └── feature_columns.json
└── eth_usdt/                        ← Модели для ETH (если обучали)
    └── ...
```

### Проверить текущие модели:

```bash
# На Windows
cd d:\Projects\AiCryptoBot
ls models/

# На VPS
cd /opt/aicryptobot
ls -lh models/
```

### Информация о модели:

```python
# В Python
import joblib

model = joblib.load('models/random_forest_model.joblib')
print(f"Estimators: {model.n_estimators}")
print(f"Max depth: {model.max_depth}")
print(f"Features: {model.n_features_in_}")
```

---

## 🎯 Текущая проблема: Модели НЕ ОБНОВЛЯЮТСЯ автоматически!

### ❌ Что НЕ происходит сейчас:

- ❌ Модель НЕ переобучается каждый день
- ❌ Модель НЕ учится на новых данных во время работы
- ❌ Нет онлайн обучения (online learning)

### ✅ Что происходит:

- ✅ Модель **загружается** при запуске
- ✅ Делает предсказания на основе **старого обучения**
- ✅ Работает с теми знаниями, что получила при обучении

**Простыми словами:**
Модель - это как **замороженный снимок** знаний на момент обучения.
Если обучили 1 декабря на данных за ноябрь, то она будет использовать только эти знания до следующего переобучения.

---

## 🔄 Как дообучить модели (Retraining)

### Вариант 1: Ручное переобучение (сейчас доступно)

#### На локальной машине:

```bash
# 1. Зайти в проект
cd d:\Projects\AiCryptoBot

# 2. Активировать venv
.\venv\Scripts\activate

# 3. Запустить скрипт обучения
python scripts/train_ensemble.py
```

**Что произойдет:**
1. Скачает свежие данные с биржи (2000 последних свечей)
2. Рассчитает индикаторы
3. Обучит Random Forest
4. Обучит LSTM (если установлен torch)
5. Сохранит модели в `models/btc_usdt/`

**Время:** 5-15 минут в зависимости от железа

#### На VPS:

```bash
# 1. Подключиться
ssh root@85.209.134.246

# 2. Зайти в проект
cd /opt/aicryptobot

# 3. Активировать venv
source venv/bin/activate

# 4. Запустить обучение
python scripts/train_ensemble.py

# 5. Перезапустить бота с новой моделью
systemctl restart aibot-dashboard
```

---

### Вариант 2: Автоматическое переобучение (нужно добавить)

Можно настроить **cron job** для автоматического переобучения:

```bash
# Создать скрипт
nano /opt/aicryptobot/scripts/auto_retrain.sh
```

```bash
#!/bin/bash
cd /opt/aicryptobot
source venv/bin/activate

# Обучить модели
python scripts/train_ensemble.py

# Перезапустить бота
systemctl restart aibot-dashboard
```

```bash
# Сделать исполняемым
chmod +x /opt/aicryptobot/scripts/auto_retrain.sh

# Добавить в crontab (каждое воскресенье в 3:00)
crontab -e
```

Добавить строку:
```cron
0 3 * * 0 /opt/aicryptobot/scripts/auto_retrain.sh >> /opt/aicryptobot/logs/retrain.log 2>&1
```

**Теперь каждую неделю в 3 утра:**
1. ✅ Скачает свежие данные
2. ✅ Переобучит модели
3. ✅ Перезапустит бота с новыми моделями

---

### Вариант 3: Online Learning (Инкрементальное обучение)

**Что это:**
Модель учится **постепенно** на каждой новой сделке.

**Преимущества:**
- ✅ Адаптируется к рынку в реальном времени
- ✅ Не нужно хранить всю историю
- ✅ Быстрее, чем полное переобучение

**Недостатки:**
- ⚠️ Может забыть старые паттерны
- ⚠️ Риск переобучения на шуме
- ⚠️ Нужны специальные алгоритмы

**Для нашего бота можно добавить:**

```python
# В src/ml/predictor.py
def partial_fit(self, X_new, y_new):
    """
    Инкрементальное обучение на новых данных
    
    ⚠️ Работает только с некоторыми моделями:
    - SGDClassifier (вместо RandomForest)
    - MLPClassifier (нейронная сеть)
    """
    if hasattr(self.model, 'partial_fit'):
        # Модель поддерживает инкрементальное обучение
        self.model.partial_fit(X_new, y_new)
    else:
        # Нужно полное переобучение
        raise NotImplementedError("Model doesn't support partial_fit")
```

**Random Forest НЕ поддерживает online learning!**

Нужно заменить на:
- `SGDClassifier` (Stochastic Gradient Descent)
- `PassiveAggressiveClassifier`
- `MLPClassifier` (Neural Network)

---

## 🆚 Online Learning vs Retraining

### Сравнение подходов:

| Критерий | Retraining (сейчас) | Online Learning |
|----------|---------------------|-----------------|
| **Частота** | Раз в неделю/месяц | После каждой сделки |
| **Данные** | Вся история (2000 свечей) | Только новые данные |
| **Время** | 5-15 минут | Секунды |
| **Память** | Нужна вся история | Только текущее состояние |
| **Забывание** | Нет | Да, забывает старое |
| **Стабильность** | Высокая | Может быть нестабильной |
| **Адаптация** | Медленная | Быстрая |
| **Риск переобучения** | Низкий | Высокий |

---

## 💡 Рекомендации

### Для вашего случая (Начинающий трейдер):

**Лучший подход: Гибрид**

1. **Еженедельное переобучение** (Retraining)
   - Каждое воскресенье обновлять модели
   - Использовать свежие 2000 свечей
   - Стабильно и надежно

2. **Мониторинг качества**
   - Смотреть на win rate
   - Если упал < 50% → срочное переобучение

3. **В будущем**: Добавить Online Learning
   - Когда наберется опыт
   - Для быстрой адаптации к новым условиям

---

## 🔧 Практические команды

### Проверить когда модель обучалась:

```bash
# На VPS
ls -lh /opt/aicryptobot/models/random_forest_model.joblib
# Дата последнего изменения = дата обучения
```

### Обучить сейчас:

```bash
# Локально (Windows)
cd d:\Projects\AiCryptoBot
python scripts/train_ensemble.py

# VPS
ssh root@85.209.134.246
cd /opt/aicryptobot
source venv/bin/activate
python scripts/train_ensemble.py
```

### Настроить автоматическое переобучение:

```bash
# На VPS - добавить в cron
0 3 * * 0 cd /opt/aicryptobot && source venv/bin/activate && python scripts/train_ensemble.py && systemctl restart aibot-dashboard
```

### Посмотреть метрики модели:

```python
# В Python
import joblib
import json

# Random Forest
model = joblib.load('models/random_forest_model.joblib')
print(f"Accuracy: {model.score(X_test, y_test):.2%}")

# LSTM (если есть)
with open('models/btc_usdt/training_metrics.json') as f:
    metrics = json.load(f)
    print(metrics)
```

---

## 📊 Как понять что модель устарела?

### Признаки устаревания:

1. **Win Rate падает**
   ```bash
   # Проверить в БД
   sqlite3 data/trading.db
   SELECT 
       COUNT(*) as total,
       SUM(CASE WHEN pnl > 0 THEN 1 ELSE 0 END) as wins,
       100.0 * SUM(CASE WHEN pnl > 0 THEN 1 ELSE 0 END) / COUNT(*) as win_rate
   FROM trades
   WHERE timestamp > datetime('now', '-7 days');
   ```

2. **ML Confidence низкая**
   ```bash
   # В логах видно много:
   [SIGNAL] ML confidence too low (58%)
   [SIGNAL] NO TRADE - Low confidence
   ```

3. **Много ложных сигналов**
   ```bash
   # Открывает сделки, которые сразу в минус
   [TRADE] Opened LONG @ $95,000
   [CLOSE] Stop Loss @ $93,500 (-1.6%)
   ```

4. **Модель старше 30 дней**
   ```bash
   # Если файл не обновлялся месяц
   ls -lh models/
   ```

---

## ✅ Чеклист "Модель в хорошем состоянии"

- ✅ Win Rate > 55%
- ✅ ML Confidence обычно > 70%
- ✅ Модель обновлялась < 14 дней назад
- ✅ Accuracy на валидации > 60%
- ✅ Нет резких изменений в рынке (крах, памп)
- ✅ Логи показывают уверенные предсказания

---

## 🎯 Итог

### Текущее состояние:
- ✅ **Модели обучены** (есть random_forest_model.joblib)
- ✅ **Работают** (делают предсказания)
- ❌ **НЕ обновляются** автоматически
- ❌ **НЕ учатся** на новых данных во время работы

### Качество обучения:
- ⚠️ **Зависит от даты последнего обучения**
- ⚠️ **Может устареть** через 2-4 недели
- ✅ **Проверить можно** через win rate и accuracy

### Как "подлить знаний":
1. **Ручное переобучение**: `python scripts/train_ensemble.py` ✅ ДОСТУПНО СЕЙЧАС
2. **Автоматическое** (cron): Еженедельно в 3:00 ✅ ЛЕГКО ДОБАВИТЬ
3. **Online Learning**: Требует переделки моделей ⚠️ СЛОЖНО

### Рекомендация:
**Настроить еженедельное автоматическое переобучение** через cron!

Это даст:
- ✅ Свежие знания каждую неделю
- ✅ Адаптация к изменениям рынка
- ✅ Не нужно помнить о ручном обучении
- ✅ Минимум работы - максимум результата

---

## 🚀 Следующий шаг:

```bash
# Настроить автоматическое переобучение
ssh root@85.209.134.246

# Создать скрипт
cat > /opt/aicryptobot/scripts/auto_retrain.sh << 'EOF'
#!/bin/bash
cd /opt/aicryptobot
source venv/bin/activate
python scripts/train_ensemble.py
systemctl restart aibot-dashboard
EOF

chmod +x /opt/aicryptobot/scripts/auto_retrain.sh

# Добавить в cron (каждое воскресенье в 3:00)
(crontab -l 2>/dev/null; echo "0 3 * * 0 /opt/aicryptobot/scripts/auto_retrain.sh >> /opt/aicryptobot/logs/retrain.log 2>&1") | crontab -

echo "✅ Автоматическое переобучение настроено!"
```

Теперь модели будут **всегда свежими**! 🎉

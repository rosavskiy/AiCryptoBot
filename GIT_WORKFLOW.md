# 🔄 Git Workflow - Development & Deployment

## Рабочий процесс

```
Local (Windows)  →  GitHub  →  VPS (Production)
    ↓                  ↓            ↓
 Разработка        Коммиты      Деплой
```

---

## 📝 На локальной машине (Windows)

### Первичная настройка Git:

```powershell
cd d:\Projects\AiCryptoBot

# Инициализировать репозиторий (если еще не сделано)
git init
git remote add origin https://github.com/rosavskiy/AiCryptoBot.git

# Добавить .gitignore (если нет)
git add .gitignore
```

### Проверьте .gitignore:

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log

# Data
data/
*.db
*.sqlite

# Models
models/*.pkl
models/*.joblib
models/*.pth
models/*.h5

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Temporary
tmp/
temp/
*.tmp

# Secrets
*.pem
*.key
*.crt
config/api_keys.yaml
```

### Ежедневный workflow:

```powershell
# 1. Проверить статус
git status

# 2. Добавить изменения
git add .

# 3. Коммит с сообщением
git commit -m "feat: добавлена новая фича"

# 4. Отправить на GitHub
git push origin main
```

### Типы коммитов (conventional commits):

```bash
feat: новая функциональность
fix: исправление бага
docs: изменения в документации
refactor: рефакторинг кода
perf: улучшение производительности
test: добавление тестов
chore: обновление зависимостей
```

**Примеры:**
```bash
git commit -m "feat: добавлен FinBERT sentiment analyzer"
git commit -m "fix: исправлена ошибка в расчете stop-loss"
git commit -m "docs: обновлена инструкция по деплою"
git commit -m "perf: оптимизирован расчет индикаторов"
```

---

## 🚀 На VPS (автоматический деплой)

### Первичная настройка (один раз):

```bash
# 1. Подключиться к VPS
ssh root@85.209.134.246

# 2. Запустить setup_vps.sh (если еще не сделано)
chmod +x setup_vps.sh
./setup_vps.sh

# 3. Клонировать репозиторий
cd /opt/aicryptobot
git clone https://github.com/rosavskiy/AiCryptoBot.git .

# 4. Настроить .env
cp .env.example .env
nano .env
# Добавить API ключи

# 5. Установить зависимости
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 6. Запустить сервис
sudo systemctl start aibot-dashboard
sudo systemctl enable aibot-dashboard
```

### Автоматическое обновление (каждый раз):

```bash
# Подключиться к VPS
ssh root@85.209.134.246

# Запустить скрипт обновления
cd /opt/aicryptobot
bash deploy_from_git.sh
```

**Скрипт автоматически:**
1. ✅ Остановит бота
2. ✅ Сделает backup .env
3. ✅ Подтянет последние изменения с GitHub
4. ✅ Восстановит .env
5. ✅ Обновит зависимости
6. ✅ Перезапустит бота
7. ✅ Покажет статус и логи

---

## 🔄 Полный цикл разработки

### Сценарий 1: Изменение параметров

```powershell
# На Windows:
# 1. Открыть config/settings.yaml
# 2. Изменить параметры (например, risk_per_trade)
# 3. Закоммитить и запушить

git add config/settings.yaml
git commit -m "chore: уменьшил risk_per_trade до 0.5%"
git push origin main
```

```bash
# На VPS:
ssh root@85.209.134.246
cd /opt/aicryptobot
bash deploy_from_git.sh

# Готово! Изменения применены
```

### Сценарий 2: Добавление новой функции

```powershell
# На Windows:
# 1. Создать новый файл или изменить существующий
# 2. Протестировать локально
# 3. Закоммитить

git add src/ml/new_feature.py
git commit -m "feat: добавлен новый ML индикатор"
git push origin main
```

```bash
# На VPS:
bash deploy_from_git.sh
```

### Сценарий 3: Обновление зависимостей

```powershell
# На Windows:
pip install новый_пакет
pip freeze > requirements.txt

git add requirements.txt
git commit -m "chore: добавлен пакет numpy"
git push origin main
```

```bash
# На VPS:
bash deploy_from_git.sh
# Скрипт автоматически установит новые пакеты
```

---

## 🛡️ Безопасность .env

**.env НЕ должен быть в Git!**

### На локальной машине:

```powershell
# .env должен быть в .gitignore
# Проверить:
git check-ignore .env
# Должно вывести: .env

# Если .env случайно добавлен:
git rm --cached .env
git commit -m "chore: удалил .env из git"
```

### На VPS:

```bash
# .env создается вручную и никогда не перезаписывается
# deploy_from_git.sh делает backup перед обновлением

# Посмотреть backups:
ls -la /opt/aicryptobot/.env.backup.*

# Восстановить из backup (если нужно):
cp .env.backup.20250101_120000 .env
```

---

## 📊 Мониторинг после деплоя

### Проверка статуса:

```bash
# Статус сервиса
sudo systemctl status aibot-dashboard

# Логи в реальном времени
tail -f /opt/aicryptobot/logs/dashboard.log

# Последние 50 строк
sudo journalctl -u aibot-dashboard -n 50

# Открыть dashboard
# http://85.209.134.246:5000
```

### Откат (если что-то пошло не так):

```bash
# Откатиться на предыдущую версию
cd /opt/aicryptobot
git log --oneline -5  # Посмотреть последние коммиты
git reset --hard HEAD~1  # Откатиться на 1 коммит назад

# ИЛИ откатиться на конкретный коммит
git reset --hard <commit_hash>

# Восстановить зависимости и перезапустить
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart aibot-dashboard
```

---

## 🚀 Быстрые команды

### На Windows (разработка):

```powershell
# Быстрый коммит и пуш
git add .
git commit -m "fix: исправление"
git push

# Посмотреть историю
git log --oneline -10

# Проверить статус
git status
```

### На VPS (деплой):

```bash
# Один скрипт для всего
ssh root@85.209.134.246 "cd /opt/aicryptobot && bash deploy_from_git.sh"

# ИЛИ создать алиас для удобства
alias deploy='ssh root@85.209.134.246 "cd /opt/aicryptobot && bash deploy_from_git.sh"'

# Теперь просто:
deploy
```

---

## 🔧 Настройка SSH для удобства (опционально)

### На Windows создать ~/.ssh/config:

```
Host aibot
    HostName 85.209.134.246
    User root
    IdentityFile ~/.ssh/id_rsa
```

Теперь можно подключаться просто:
```powershell
ssh aibot
```

И деплоить:
```powershell
ssh aibot "cd /opt/aicryptobot && bash deploy_from_git.sh"
```

---

## 📋 Чеклист перед каждым деплоем

- [ ] Код работает локально
- [ ] Все файлы закоммичены
- [ ] .env НЕ в git
- [ ] requirements.txt обновлен (если добавлены пакеты)
- [ ] Документация обновлена (если нужно)
- [ ] Тесты пройдены (если есть)
- [ ] Коммит message информативный

---

## 🎯 Workflow в действии

### Типичный день разработки:

```powershell
# Утро - начало работы
cd d:\Projects\AiCryptoBot
git pull  # На всякий случай

# Разработка...
# Изменил src/ml/predictor.py
# Добавил новый индикатор

# Тестирование локально
python test_system.py

# Коммит
git add src/ml/predictor.py
git commit -m "feat: добавлен индикатор momentum"
git push origin main

# Деплой на VPS
ssh root@85.209.134.246 "cd /opt/aicryptobot && bash deploy_from_git.sh"

# Проверка
# Открыть http://85.209.134.246:5000
# Посмотреть логи

# Готово! 🎉
```

---

**Workflow настроен! Теперь разработка → Git → VPS максимально автоматизирована 🚀**

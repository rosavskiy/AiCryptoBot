# 📜 Просмотр исторических логов

## Что добавлено

### ✅ Функционал

1. **Персистентные логи в файл**
   - Путь: `logs/dashboard.log`
   - Формат: `[timestamp] [LEVEL] message`
   - Кодировка: UTF-8
   - Автоматическое логирование всех событий

2. **API endpoint для чтения истории**
   - URL: `GET /api/logs/history`
   - Возвращает: последние 500 строк из файла
   - Формат: JSON с парсингом и категоризацией
   - Категории: news, ml, trade, error, general

3. **Кнопка "Загрузить историю" в UI**
   - Вкладка: "Логи"
   - Действие: Загружает историю из файла
   - Индикатор: Спиннер во время загрузки
   - Уведомление: Количество загруженных логов

4. **Автоматическая загрузка при открытии вкладки**
   - Загружается 1 раз при первом открытии
   - Последующие открытия используют кэш
   - Сброс кэша при очистке логов

## 🎯 Использование

### В браузере (Dashboard):

1. **Открыть вкладку "Логи"**
   - История автоматически загрузится (если есть файл)
   - Или нажать кнопку "Загрузить историю"

2. **Фильтровать логи**
   - Все / Новости / ML / Сделки / Ошибки
   - Работает с исторической и текущей сессией

3. **Экспортировать**
   - Кнопка "Экспорт"
   - Скачивается .txt файл со всеми логами

4. **Очистить**
   - Кнопка "Очистить"
   - Очищает только отображение (файл сохраняется!)

### На VPS (SSH):

```bash
# Смотреть логи в реальном времени
tail -f /opt/aicryptobot/logs/dashboard.log

# Последние 100 строк
tail -n 100 /opt/aicryptobot/logs/dashboard.log

# Поиск по ключевым словам
grep "NEWS" /opt/aicryptobot/logs/dashboard.log
grep "ERROR" /opt/aicryptobot/logs/dashboard.log
grep "TRADE" /opt/aicryptobot/logs/dashboard.log

# Логи за последний час
tail -n 1000 /opt/aicryptobot/logs/dashboard.log | grep "$(date +%H:)"

# Экспорт в файл
tail -n 5000 /opt/aicryptobot/logs/dashboard.log > my_logs.txt
```

### Через systemd (journalctl):

```bash
# Системные логи (полная история)
journalctl -u aibot-dashboard

# Последние 100 строк
journalctl -u aibot-dashboard -n 100

# За последний час
journalctl -u aibot-dashboard --since "1 hour ago"

# За сегодня
journalctl -u aibot-dashboard --since today

# В реальном времени
journalctl -u aibot-dashboard -f

# Фильтр по уровню
journalctl -u aibot-dashboard -p err  # Только ошибки
journalctl -u aibot-dashboard -p warning  # Warning и выше

# Экспорт
journalctl -u aibot-dashboard > full_logs.txt
```

## 🔄 Что сохраняется и что теряется

### ✅ Сохраняется после перезагрузки страницы:

- ✅ Логи в файле `logs/dashboard.log`
- ✅ Логи в systemd journalctl
- ✅ История сделок в БД `trading.db`
- ✅ Состояние бота (работает на VPS)

### ❌ Теряется после перезагрузки страницы:

- ❌ Логи в памяти JavaScript (массив `allLogs`)
- ❌ Текущие WebSocket соединения

### Что происходит при закрытии браузера:

1. **Бот продолжает работать** ✅
   - Служба systemd на VPS работает 24/7
   - Торговля не прерывается
   - Логи продолжают записываться

2. **При новом открытии dashboard:**
   - Нажать "Загрузить историю" → увидите все пропущенные логи
   - Или автоматически загрузится при открытии вкладки "Логи"
   - Текущие данные (P&L, позиции) обновятся из БД

3. **При открытии с другого компьютера:**
   - Тот же результат - вся история доступна
   - Кнопка "Загрузить историю" синхронизирует состояние

## 📊 Примеры логов

### Файл logs/dashboard.log:
```
2025-12-01 16:00:00,123 [INFO] [WEB] Starting web dashboard
2025-12-01 16:00:05,456 [INFO] [NEWS] 📝 Using TextBlob for sentiment
2025-12-01 16:15:03,567 [INFO] [NEWS] 📰 Новость: Bitcoin reaches new high (sentiment: 0.85 😊)
2025-12-01 16:15:05,123 [INFO] [ML] 🧠 Model prediction: LONG with confidence 0.78
2025-12-01 16:15:06,456 [INFO] [TRADE] 🎯 Opening LONG position: BTC/USDT @ 95000
2025-12-01 16:25:00,345 [ERROR] [TRADE] ❌ Failed to close position: Network timeout
```

### API Response /api/logs/history:
```json
{
  "logs": [
    {
      "timestamp": "2025-12-01 16:00:00",
      "level": "INFO",
      "category": "general",
      "message": "[WEB] Starting web dashboard"
    },
    {
      "timestamp": "2025-12-01 16:15:03",
      "level": "INFO",
      "category": "news",
      "message": "[NEWS] 📰 Новость: Bitcoin reaches new high"
    },
    {
      "timestamp": "2025-12-01 16:15:05",
      "level": "INFO",
      "category": "ml",
      "message": "[ML] 🧠 Model prediction: LONG"
    }
  ],
  "total": 500
}
```

## 🔧 Конфигурация

### Изменить размер истории:

В `src/web/app.py`:
```python
# Строка 219 - изменить с 500 на нужное значение
last_lines = lines[-1000:] if len(lines) > 1000 else lines
```

### Изменить путь к логам:

В `src/web/app.py`:
```python
# Строка 24-25
log_file = log_dir / 'dashboard.log'
```

### Ротация логов (опционально):

Для production рекомендуется настроить ротацию:

```python
# Добавить в src/web/app.py
from logging.handlers import RotatingFileHandler

file_handler = RotatingFileHandler(
    log_file,
    maxBytes=10*1024*1024,  # 10 MB
    backupCount=5,
    encoding='utf-8'
)
```

## 🚀 Деплой

### 1. Коммит изменений:
```bash
git add -A
git commit -m "feat: добавлен просмотр исторических логов

- Персистентные логи в файл logs/dashboard.log
- API endpoint /api/logs/history для чтения истории
- Кнопка 'Загрузить историю' в UI
- Автоматическая загрузка при открытии вкладки Логи
- Парсинг и категоризация логов (news/ml/trade/error)
- Сохранение последних 500 строк"

git push origin main
```

### 2. Деплой на VPS:
```bash
ssh root@85.209.134.246
cd /opt/aicryptobot
bash deploy_from_git.sh
```

### 3. Проверка:
```bash
# Должен создаться файл логов
ls -lh /opt/aicryptobot/logs/dashboard.log

# Проверить API
curl http://127.0.0.1:5000/api/logs/history | python -m json.tool

# Открыть dashboard и нажать "Загрузить историю"
```

## 💡 Рекомендации

### Для development (Windows):
- Логи в `d:\Projects\AiCryptoBot\logs\dashboard.log`
- Используйте кнопку "Загрузить историю" для тестирования
- Файл накапливается, можно очищать вручную

### Для production (VPS):
- Логи в `/opt/aicryptobot/logs/dashboard.log`
- Настройте ротацию логов (см. выше)
- Используйте `journalctl` для системных логов
- Файловые логи для детального анализа

### Best practices:
1. **Регулярно проверяйте размер** `ls -lh logs/dashboard.log`
2. **Архивируйте старые логи**: `gzip logs/dashboard.log.old`
3. **Мониторьте ошибки**: `grep ERROR logs/dashboard.log`
4. **Экспортируйте важное**: Используйте кнопку "Экспорт" в UI

## ✅ Проверка работы

### Тест 1: Создание логов
```bash
# Запустить dashboard
python run_dashboard.py

# Проверить файл создался
ls logs/dashboard.log

# Должно быть содержимое
cat logs/dashboard.log
```

### Тест 2: API endpoint
```bash
# В другом терминале
curl http://localhost:5000/api/logs/history
```

Должен вернуть JSON с логами.

### Тест 3: UI кнопка
1. Открыть http://localhost:5000
2. Вкладка "Логи"
3. Нажать "Загрузить историю"
4. Должны появиться логи из файла

### Тест 4: Персистентность
1. Открыть dashboard
2. Запустить бота → появятся логи
3. Закрыть браузер
4. Снова открыть dashboard
5. Нажать "Загрузить историю"
6. ✅ Все предыдущие логи должны отобразиться

## 🎉 Итог

Теперь вы можете:
- ✅ Смотреть историю логов после перезагрузки
- ✅ Открывать dashboard с любого компьютера и видеть полную историю
- ✅ Фильтровать логи по категориям
- ✅ Экспортировать логи в файл
- ✅ Не терять данные при закрытии браузера

Бот работает независимо от dashboard! 🚀

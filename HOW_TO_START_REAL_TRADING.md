# 🚨 ВАЖНО: Бот работает в ДЕМО режиме!

## Проблемы которые вы заметили:

### 1. ⏰ Время работы показывает 4 часа (должно быть ~1 час)

**Причина:** Логи писались в UTC (серверное время), а дашборд думал что это локальное время.

**Разница:**
```
Сервер (UTC):      13:26 (запуск)
Ваше (UTC+3):      16:26
Бот работает:      ~1 час
Показывает:        4 часа (из-за разницы часовых поясов)
```

**✅ ИСПРАВЛЕНО:** Логи теперь пишутся в локальном времени сервера.

---

### 2. 🤖 Бот ничего не делает 15 минут!

**Причина:** Бот работает в **ДЕМО РЕЖИМЕ**!

Сейчас запущен только **веб-дашборд** (`run_dashboard.py`), но НЕ сам торговый бот!

**Что работает:**
- ✅ Веб-интерфейс
- ✅ WebSocket соединения
- ✅ News Scheduler (загрузка новостей каждые 15 мин)
- ❌ Торговый бот (ML, анализ, сделки)

**В логах только:**
```
[WEB] Starting web dashboard
[WEB] Client connected / disconnected
[NEWS] Scheduler started
```

**НЕТ:**
```
[ML] Analyzing market...
[SIGNAL] BUY signal detected
[TRADE] Opened position
```

---

## 🔧 Как запустить НАСТОЯЩИЙ бот

### Вариант 1: Запустить торговый бот на VPS

```bash
ssh root@85.209.134.246

# Остановить демо-дашборд
systemctl stop aibot-dashboard

# Запустить настоящий бот
cd /opt/aicryptobot
source venv/bin/activate

# ВАЖНО: Проверить API ключи!
nano config/config.yaml
# Убедиться что есть:
# exchange:
#   api_key: "ваш_ключ"
#   secret: "ваш_секрет"

# Запустить бота (с веб-дашбордом)
python main.py --mode live --web-dashboard
```

**Что произойдет:**
1. Бот подключится к Binance
2. Начнет анализировать рынок каждые 15 минут
3. ML модель будет делать предсказания
4. При сигнале откроет сделку
5. Веб-дашборд будет показывать реальные данные

---

### Вариант 2: Сначала протестировать (Backtest)

**Рекомендую начать с этого!**

```bash
ssh root@85.209.134.246
cd /opt/aicryptobot
source venv/bin/activate

# Бэктест на исторических данных
python main.py --mode backtest --days 30

# Посмотреть результаты
cat logs/backtest_*.log
```

**Зачем:**
- Увидите как бот торгует на истории
- Оцените прибыль/убытки
- Проверите что всё работает
- **БЕЗ РИСКА** реальных денег

---

### Вариант 3: Paper Trading (бумажные деньги)

```bash
ssh root@85.209.134.246
cd /opt/aicryptobot
source venv/bin/activate

# В config.yaml поставить:
# trading:
#   mode: paper  # <-- Виртуальные деньги

python main.py --mode live --web-dashboard
```

**Что даёт:**
- Бот работает в реальном времени
- Подключается к реальной бирже
- Получает реальные цены
- **НО торгует виртуально** (не тратит деньги)

---

## ⚠️ Почему сейчас ДЕМО режим?

Смотрю на ваш systemd service:

```bash
# /etc/systemd/system/aibot-dashboard.service
ExecStart=/opt/aicryptobot/venv/bin/python run_dashboard.py
```

Это запускает **только веб-интерфейс**, не торгового бота!

**Для настоящей торговли нужно:**

```bash
# Вариант 1: Изменить service
sudo nano /etc/systemd/system/aibot-dashboard.service

# Заменить:
ExecStart=/opt/aicryptobot/venv/bin/python main.py --mode live --web-dashboard

# Перезагрузить
sudo systemctl daemon-reload
sudo systemctl restart aibot-dashboard
```

**Или Вариант 2: Создать отдельный service для бота**

```bash
sudo nano /etc/systemd/system/aibot-trading.service
```

```ini
[Unit]
Description=AI Crypto Bot Trading
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/aicryptobot
Environment="PATH=/opt/aicryptobot/venv/bin"
ExecStart=/opt/aicryptobot/venv/bin/python main.py --mode live
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
# Активировать
sudo systemctl daemon-reload
sudo systemctl enable aibot-trading
sudo systemctl start aibot-trading

# Проверить
sudo systemctl status aibot-trading
tail -f /opt/aicryptobot/logs/trading_*.log
```

---

## 🎯 Что нужно сделать ПЕРЕД запуском реального бота

### 1. Проверить API ключи

```bash
ssh root@85.209.134.246
cat /opt/aicryptobot/config/config.yaml | grep -A 5 "exchange:"
```

**Должно быть:**
```yaml
exchange:
  name: binance
  api_key: "ваш_реальный_api_key"
  secret: "ваш_реальный_secret"
```

**Если нет ключей:**
1. Зайти на Binance → API Management
2. Создать API ключ
3. Скопировать Key и Secret
4. Вставить в config.yaml

---

### 2. Проверить баланс

```bash
# В Python
python -c "
from src.exchange.binance_api import BinanceAPI
api = BinanceAPI()
balance = api.get_balance()
print(f'Balance: {balance}')
"
```

---

### 3. Обучить модели (ОБЯЗАТЕЛЬНО!)

```bash
cd /opt/aicryptobot
source venv/bin/activate
python scripts/train_ensemble.py
```

**Без этого бот будет торговать со старыми/нет моделями!**

---

### 4. Настроить риск

В `config.yaml`:

```yaml
risk:
  risk_per_trade: 0.01  # 1% от баланса на сделку (рекомендую начать с 0.005 = 0.5%)
  max_open_positions: 1  # Только 1 открытая позиция (безопасно для начала)
  max_daily_loss: 0.02   # Макс 2% потерь в день
```

---

## 📊 Как понять что бот работает РЕАЛЬНО

### В логах должно быть:

```
[EXCHANGE] Connected to Binance
[ML] Loading models...
[ML] Models loaded successfully
[MARKET] Fetching BTC/USDT data...
[ML] Analyzing market conditions...
[ML] Prediction: BUY (confidence: 72%)
[SENTIMENT] News sentiment: 0.65 (positive)
[SIGNAL] BUY signal confirmed
[RISK] Position size: 0.005 BTC ($485)
[TRADE] Opening LONG @ $97,000
[TRADE] Stop Loss: $95,135 (-1.92%)
[TRADE] Take Profit: $100,910 (+4.03%)
```

### В дашборде должно быть:

- **Открытые позиции** > 0
- **Сделки** меняются
- **P&L график** двигается
- **Последний сигнал** показывает ML результаты

---

## 🚀 Быстрый старт (рекомендованный)

```bash
# 1. Подключиться к VPS
ssh root@85.209.134.246

# 2. Остановить демо-дашборд
systemctl stop aibot-dashboard

# 3. Обучить модели
cd /opt/aicryptobot
source venv/bin/activate
python scripts/train_ensemble.py

# 4. Сделать БЭКТЕСТ (проверка на истории)
python main.py --mode backtest --days 30

# 5. Посмотреть результаты
# Если Win Rate > 55% и Sharpe > 1.0 → можно пробовать live

# 6. Запустить Paper Trading (виртуальные деньги)
# В config.yaml: trading.mode = paper
python main.py --mode live --web-dashboard &

# 7. Наблюдать 1-2 дня
# Если всё работает стабильно → переключить на real trading
```

---

## 🔍 Текущая ситуация

```
┌─────────────────────────────────────┐
│  ВАШ БОТ СЕЙЧАС                    │
├─────────────────────────────────────┤
│  Режим:      DEMO (только UI)      │
│  Торговля:   НЕТ                   │
│  ML:         НЕ РАБОТАЕТ           │
│  Сигналы:    НЕТ                   │
│  Сделки:     0                     │
│                                     │
│  Работает:                         │
│  ✅ Веб-интерфейс                  │
│  ✅ News Scheduler                 │
│  ✅ WebSocket                      │
│                                     │
│  Не работает:                      │
│  ❌ Trading Bot                    │
│  ❌ ML Predictor                   │
│  ❌ Signal Generator               │
│  ❌ Trade Executor                 │
└─────────────────────────────────────┘
```

---

## ✅ Исправления которые я сделал

1. **Время в логах** - теперь локальное, не UTC
2. **Автозагрузка истории логов** - работает
3. **Объяснение Client disconnected** - это норма

---

## 🎯 Следующий шаг

**ВЫБРАТЬ:**

### Вариант A: Запустить реальную торговлю
```bash
# ОСТОРОЖНО! Это реальные деньги
python main.py --mode live --web-dashboard
```

### Вариант B: Бэктест (безопасно)
```bash
# Проверить на истории
python main.py --mode backtest --days 30
```

### Вариант C: Paper Trading (рекомендую)
```bash
# Виртуальные деньги, реальный рынок
# В config.yaml: trading.mode = paper
python main.py --mode live --web-dashboard
```

**Рекомендую: B → C → A** (сначала тест, потом виртуально, потом реально)

# 🔐 GitHub Personal Access Token - Инструкция

Для клонирования приватного репозитория на VPS.

---

## Шаг 1: Создать токен на GitHub

1. Зайти: https://github.com/settings/tokens
2. Нажать **"Generate new token"** → **"Generate new token (classic)"**
3. Настроить:
   - **Note**: `VPS Deploy Token`
   - **Expiration**: 90 days (или No expiration для удобства)
   - **Select scopes**: 
     - ✅ `repo` - полный доступ к приватным репозиториям
4. Нажать **"Generate token"**
5. **ВАЖНО**: Скопировать токен! Он показывается только один раз!

Токен будет выглядеть так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

## Шаг 2: Использовать токен для клонирования

### На VPS:

```bash
cd /opt/aicryptobot

# Формат: https://TOKEN@github.com/username/repo.git
git clone https://ghp_ваш_токен_здесь@github.com/rosavskiy/AiCryptoBot.git .
```

**Пример:**
```bash
git clone https://ghp_1A2B3C4D5E6F7G8H9I0J@github.com/rosavskiy/AiCryptoBot.git .
```

---

## Шаг 3: Настроить git для pull

Чтобы `git pull` работал в будущем:

```bash
cd /opt/aicryptobot

# Сохранить токен в git credentials
git config credential.helper store

# Теперь при первом pull введите:
# Username: rosavskiy
# Password: ghp_ваш_токен

# Или сразу установить remote с токеном:
git remote set-url origin https://ghp_ваш_токен@github.com/rosavskiy/AiCryptoBot.git
```

---

## Альтернатива: Сделать репо публичным

Если не хотите возиться с токенами:

1. Зайти: https://github.com/rosavskiy/AiCryptoBot/settings
2. Прокрутить вниз до **"Danger Zone"**
3. **"Change visibility"** → **"Make public"**
4. Подтвердить

⚠️ **Внимание**: Не делайте репо публичным, если там есть:
- API ключи (должны быть в .env, который в .gitignore)
- Пароли или токены
- Другие секреты

---

## Проверка: что НЕ должно быть в Git

Убедитесь, что `.gitignore` исключает:

```
.env
.env.local
.env.*.local
.env.backup.*
logs/
data/
*.db
models/*.pkl
config/api_keys.yaml
```

Проверить:
```powershell
# На Windows:
git status

# Не должно показывать .env файл!
```

---

## Безопасность токена

✅ **DO:**
- Храните токен в безопасном месте
- Используйте expiration date
- Давайте минимальные права (только `repo`)
- Удаляйте старые токены

❌ **DON'T:**
- НЕ коммитьте токен в код
- НЕ делитесь токеном
- НЕ храните в plaintext файлах

---

## Если токен утёк

1. Немедленно удалить на: https://github.com/settings/tokens
2. Создать новый
3. Обновить на VPS:
```bash
cd /opt/aicryptobot
git remote set-url origin https://новый_токен@github.com/rosavskiy/AiCryptoBot.git
```

---

**Готово! Теперь можете клонировать приватный репо 🔐**

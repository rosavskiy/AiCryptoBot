# 🎨 Web Dashboard - Руководство пользователя

## 🚀 Быстрый старт

### Запуск Dashboard

```bash
# Установить зависимости (если ещё не установлены)
pip install flask flask-socketio python-socketio gevent

# Запустить dashboard
python run_dashboard.py
```

Dashboard будет доступен по адресу: **http://localhost:5000**

### Запуск с ботом

```bash
# Запустить бота с web интерфейсом
python main.py --enable-web
```

---

## 📊 Обзор интерфейса

### 1. **Карточки статистики** (верхняя панель)

- **Баланс**: Текущий баланс и изменение в %
- **P&L**: Прибыль/убыток за сессию
- **Сделки**: Общее количество + Win/Loss breakdown
- **Win Rate**: Процент успешных сделок + Sharpe Ratio

### 2. **График P&L** (левая колонка)

- Динамика прибыли в реальном времени
- Обновляется каждые 5 секунд
- Отображает последние 50 точек

### 3. **Открытые позиции** (левая колонка)

Таблица с активными сделками:
- Символ
- Сторона (LONG/SHORT)
- Цена входа
- Текущая цена
- Текущий P&L ($и %)
- Время открытия

### 4. **История сделок** (левая колонка)

Последние 20 закрытых сделок:
- Время закрытия
- Символ и сторона
- Финальный P&L
- Причина закрытия (TP/SL/Time/Manual)

### 5. **Информация о боте** (правая колонка)

- **Статус**: Работает/Остановлен/Пауза
- **Время работы**: Uptime с момента запуска
- **Параметры**: Символ, интервал, ML порог, риск
- **Риск-метрики**: Max DD, Current DD

### 6. **Последний сигнал** (правая колонка)

Информация о последнем торговом сигнале:
- Время получения
- Направление (BUY/SELL)
- ML Confidence (вероятность модели)
- Sentiment Score

### 7. **Живые логи** (правая колонка)

Real-time логи с цветовой индикацией:
- 🔵 **INFO**: Информационные сообщения
- 🟢 **SUCCESS**: Успешные операции
- 🟡 **WARNING**: Предупреждения
- 🔴 **ERROR**: Ошибки

---

## 🎮 Управление ботом

### Кнопки управления (в header)

1. **▶️ Запустить** - Начать торговлю
2. **⏸️ Пауза** - Приостановить (не закрывая позиции)
3. **⏹️ Остановить** - Полностью остановить бота

### Горячие клавиши

- `Ctrl + C` - Остановить сервер
- `F5` - Обновить страницу
- `Ctrl + L` - Очистить логи (кнопка в интерфейсе)

---

## 🔌 WebSocket Real-time Updates

Dashboard использует WebSocket для обновлений в реальном времени:

- ✅ **Автоматическое подключение** при открытии страницы
- 🔄 **Автореконнект** при разрыве соединения
- ⚡ **Мгновенные обновления** при изменениях
- 📡 **Статус подключения** в футере (✓/✗)

### События WebSocket:

| Событие | Описание |
|---------|----------|
| `status_update` | Обновление общего статуса бота |
| `trade_update` | Новая сделка открыта/закрыта |
| `log_update` | Новое сообщение в логах |
| `request_update` | Запрос актуальных данных |

---

## 🎨 Темы и кастомизация

### Dark Theme (по умолчанию)

Современный тёмный дизайн с:
- Градиентными карточками
- Анимированными элементами
- Адаптивной версткой

### Цветовая схема:

```css
Фон:      #0f0f23 (тёмно-синий)
Карточки: #16213e (синий)
Акцент:   #00d9ff (голубой)
Успех:    #10b981 (зелёный)
Ошибка:   #ef4444 (красный)
```

### Адаптивность:

- 📱 **Mobile**: < 768px
- 💻 **Tablet**: 768px - 1200px
- 🖥️ **Desktop**: > 1200px

---

## 🔒 Безопасность

### Рекомендации для production:

1. **Измените SECRET_KEY** в `src/web/app.py`:
```python
app.config['SECRET_KEY'] = 'your-very-secret-key-here'
```

2. **Используйте production WSGI сервер**:
```bash
pip install gunicorn
gunicorn -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker -w 1 -b 0.0.0.0:5000 src.web.app:app
```

3. **Настройте reverse proxy** (nginx):
```nginx
location / {
    proxy_pass http://localhost:5000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

4. **Включите HTTPS** через Let's Encrypt

5. **Добавьте аутентификацию** (Flask-Login)

---

## 🐛 Troubleshooting

### Dashboard не открывается

```bash
# Проверить, запущен ли сервер
netstat -an | findstr 5000

# Проверить логи
tail -f logs/web.log
```

### WebSocket не подключается

1. Проверьте firewall
2. Убедитесь, что порт 5000 не занят
3. Попробуйте другой браузер
4. Очистите кэш браузера

### Нет обновлений в реальном времени

1. Откройте DevTools (F12) → Console
2. Проверьте ошибки WebSocket
3. Обновите страницу (F5)
4. Перезапустите сервер

### High CPU/Memory usage

```bash
# Уменьшить частоту обновлений в dashboard.js
setInterval(() => { ... }, 5000);  # Увеличить с 1000 до 5000ms
```

---

## 📊 API Endpoints

### GET Endpoints:

| Endpoint | Описание |
|----------|----------|
| `/` | Dashboard home page |
| `/api/status` | Статус бота |
| `/api/config` | Конфигурация |
| `/api/trades` | История сделок |
| `/api/performance` | Метрики производительности |
| `/api/logs` | Последние логи |

### POST Endpoints:

| Endpoint | Описание |
|----------|----------|
| `/api/control/start` | Запустить бота |
| `/api/control/stop` | Остановить бота |
| `/api/control/pause` | Пауза |

### Примеры запросов:

```bash
# Получить статус
curl http://localhost:5000/api/status

# Запустить бота
curl -X POST http://localhost:5000/api/control/start

# Получить историю сделок
curl http://localhost:5000/api/trades
```

---

## 🚀 Деплой на VPS

### Systemd Service:

Создайте `/etc/systemd/system/aibot-dashboard.service`:

```ini
[Unit]
Description=AI Crypto Bot Dashboard
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/AiCryptoBot
ExecStart=/path/to/venv/bin/python run_dashboard.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl daemon-reload
sudo systemctl enable aibot-dashboard
sudo systemctl start aibot-dashboard
```

### Docker:

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 5000
CMD ["python", "run_dashboard.py"]
```

Запуск:
```bash
docker build -t aibot-dashboard .
docker run -d -p 5000:5000 aibot-dashboard
```

---

## 📈 Мониторинг

### Метрики:

- Response time: < 100ms
- WebSocket latency: < 50ms
- Memory usage: ~ 100-200MB
- CPU usage: < 5% idle, < 20% active

### Логирование:

```python
# Настроить уровень логов
logging.basicConfig(level=logging.DEBUG)
```

---

## 🎯 Следующие шаги

1. ✅ Dashboard работает локально
2. 📦 Деплой на VPS
3. 🔒 Настроить HTTPS
4. 🔐 Добавить аутентификацию
5. 📱 Мобильная версия (PWA)
6. 📊 Расширенная аналитика
7. 🔔 Push-уведомления

---

**Dashboard готов к использованию! 🎉**

Откройте http://localhost:5000 и наслаждайтесь real-time мониторингом вашего торгового бота!
